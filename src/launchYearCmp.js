import React from "react";

export default function LaunchYearCmp() {
  const length = 2020;
  var rows = [];
  for (let i = 2016; i < length; i++) {
    console.log(i);
    return <>{rows.push({ i })}</>;
  }
}
